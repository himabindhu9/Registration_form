document.getElementByld("registrationForm").addEventListener("submit", function (e) { e.preventDefault();

// Simulating successful registration
const successMessage = document.getElementByld("successMessage"); successMessage.classList.remove("hidden'');

// Resetting the form this.reset();
});
